### Added
- Added `scripts/generate_pr_body_from_changelog.sh` to build PR descriptions from changelog fragments.
- Added optional manual workflow `.github/workflows/pr-body-from-changelog.yml` to generate and apply PR body for a selected PR.

### Changed
- Documented PR body automation usage in `README.md` and `LLM_CONTEXT.md`.
