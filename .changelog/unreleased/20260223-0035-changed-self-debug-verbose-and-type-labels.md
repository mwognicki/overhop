### Changed
- Self-debug mode now forces logger level to `VERBOSE` regardless of config/argv logging settings.
- Self-debug wire trace output now prints explicit message type names in bold (for both outgoing and incoming envelopes).
